# No Worktrees

Jordan jumping a Velotric fatbike at dusk. Banner magenta, sun gold.

Biker, not cyclist. Full clone or don't.

```bash
omarchy theme set no-worktrees
```

Palette is pulled from the jump wallpaper. Magic CLI (`magic`, `noworktrees`)
reads this `colors.toml` so the terminal matches the glass.
